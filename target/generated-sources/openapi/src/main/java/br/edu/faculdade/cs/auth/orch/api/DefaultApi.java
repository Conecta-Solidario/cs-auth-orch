/**
* NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech) (7.2.0).
* https://openapi-generator.tech
* Do not edit the class manually.
*/
package br.edu.faculdade.cs.auth.orch.api;

import org.apache.camel.builder.RouteBuilder;
import org.apache.camel.model.rest.RestParamType;
import org.springframework.stereotype.Component;
import br.edu.faculdade.cs.auth.orch.model.*;
import org.apache.camel.model.rest.RestBindingMode;
import org.apache.camel.LoggingLevel;

@Component
public class DefaultApi extends RouteBuilder {

    @Override
    public void configure() throws Exception {
        

        /**
        POST /usuarios : Cria um novo usuário
        **/
        rest()
            .securityDefinitions()
                .bearerToken("bearerAuth", "JWT").end()
            .post("/usuarios")
                .description("Cria um novo usuário")
                .id("createUserApi")
                .produces("application/json")
                .outType(UsuarioResponse.class)
                .consumes("application/json")
                .type(UsuarioRequest.class)
                
                .param()
                    .name("usuarioRequest")
                    .type(RestParamType.body)
                    .required(true)
                .endParam()
                .to("direct:createUser");
        

        /**
        DELETE /usuarios/{id} : Remove um usuário
        **/
        rest()
            .securityDefinitions()
                .bearerToken("bearerAuth", "JWT").end()
            .delete("/usuarios/{id}")
                .description("Remove um usuário")
                .id("deleteUserApi")
                .param()
                    .name("id")
                    .type(RestParamType.path)
                    .required(true)
                .endParam()
                .to("direct:deleteUser");
        

        /**
        GET /usuarios/{id} : Busca um usuário pelo ID
        **/
        rest()
            .securityDefinitions()
                .bearerToken("bearerAuth", "JWT").end()
            .get("/usuarios/{id}")
                .description("Busca um usuário pelo ID")
                .id("getUserByIdApi")
                .produces("application/json")
                .outType(UsuarioResponse.class)
                .param()
                    .name("id")
                    .type(RestParamType.path)
                    .required(true)
                .endParam()
                .to("direct:getUserById");
        

        /**
        GET /usuarios : Lista todos os usuários
        **/
        rest()
            .securityDefinitions()
                .bearerToken("bearerAuth", "JWT").end()
            .get("/usuarios")
                .description("Lista todos os usuários")
                .id("listUsersApi")
                .produces("application/json")
                .outType(UsuarioResponse[].class)
                .to("direct:listUsers");
        

        /**
        POST /login : Autentica um usuário e retorna um token JWT
        **/
        rest()
            .post("/login")
                .description("Autentica um usuário e retorna um token JWT")
                .id("loginUserApi")
                .produces("application/json")
                .outType(LoginResponse.class)
                .consumes("application/json")
                .type(LoginRequest.class)
                
                .param()
                    .name("loginRequest")
                    .type(RestParamType.body)
                    .required(true)
                .endParam()
                .to("direct:loginUser");
        

        /**
        PUT /usuarios/{id} : Atualiza um usuário existente
        **/
        rest()
            .securityDefinitions()
                .bearerToken("bearerAuth", "JWT").end()
            .put("/usuarios/{id}")
                .description("Atualiza um usuário existente")
                .id("updateUserApi")
                .produces("application/json")
                .outType(UsuarioResponse.class)
                .consumes("application/json")
                .type(UsuarioRequest.class)
                
                .param()
                    .name("id")
                    .type(RestParamType.path)
                    .required(true)
                .endParam()
                .param()
                    .name("usuarioRequest")
                    .type(RestParamType.body)
                    .required(true)
                .endParam()
                .to("direct:updateUser");
        

        /**
        POST /token/validate : Valida um token JWT
        **/
        rest()
            .post("/token/validate")
                .description("Valida um token JWT")
                .id("validateTokenApi")
                .produces("application/json")
                .outType(TokenValidationResponse.class)
                .consumes("application/json")
                .type(TokenValidationRequest.class)
                
                .param()
                    .name("tokenValidationRequest")
                    .type(RestParamType.body)
                    .required(true)
                .endParam()
                .to("direct:validateToken");
        
    }
}
